language: php

php:
  - 7.0
  - 7.1
  - hhvm

before_script:
  - "composer self-update"
  - "composer --version"
  - "composer install --no-interaction"

script:
  - "./vendor/bin/phpunit -c phpunit.xml.dist"
